# Environment variables template for air quality agent
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=airquality
DB_USER=postgres
DB_PASSWORD=password

# InstructLab Configuration
USE_INSTRUCTLAB=false  # Start with false, enable after testing
INSTRUCTLAB_ENDPOINT=http://localhost:8000
SHADOW_MODE=true  # Run LLM in parallel for comparison

# Cache Configuration
CACHE_TTL_SECONDS=3600
REDIS_HOST=localhost
REDIS_PORT=6379

# API Configuration
API_PORT=8000
STREAMLIT_PORT=8501

# Logging
LOG_LEVEL=INFO